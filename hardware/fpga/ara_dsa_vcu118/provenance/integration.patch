--- a/rtl/cheshire/hw/cheshire_pkg.sv
+++ b/rtl/cheshire/hw/cheshire_pkg.sv
@@ -493,6 +493,11 @@
     doub_bt NoCieBase = cfg.Cva6ExtCieOnTop ? 64'h2000_0000 : 64'h2000_0000 + cfg.Cva6ExtCieLength;
     // Base our config on the upstream default for this variant
     config_pkg::cva6_user_cfg_t ret = cva6_config_pkg::cva6_cfg;
+    // FPGA-only integration: retain the current scalar/vector FP16 contract.
+    // This CVA6 snapshot has stub-only FpgaEn RAMs. Use its real generic
+    // implementation; tc_sram is independently mapped to Xilinx XPM.
+    ret.FpgaEn = 0;
+    ret.XF16 = 1;
     // Modify what we need to
     ret.AxiAddrWidth          = cfg.AddrWidth;
     ret.AxiDataWidth          = cfg.AxiDataWidth;
--- a/rtl/cheshire/hw/cheshire_soc.sv
+++ b/rtl/cheshire/hw/cheshire_soc.sv
@@ -664,6 +664,16 @@
 //      .clic_irq_ready_o ( clic_irq_ready ),
 //      .clic_kill_req_i  ( clic_irq_kill_req ),
 //      .clic_kill_ack_o  ( clic_irq_kill_ack ),
+      .clic_irq_valid_i ( 1'b0 ),
+      .clic_irq_id_i    ( '0 ),
+      .clic_irq_level_i ( '0 ),
+      .clic_irq_priv_i  ( riscv::PRIV_LVL_M ),
+      .clic_irq_v_i     ( 1'b0 ),
+      .clic_irq_vsid_i  ( '0 ),
+      .clic_irq_shv_i   ( 1'b0 ),
+      .clic_irq_ready_o ( ),
+      .clic_kill_req_i  ( 1'b0 ),
+      .clic_kill_ack_o  ( ),
       .rvfi_probes_o    ( ),
       .cvxif_req_o      ( acc_req       ),
       .cvxif_resp_i     ( acc_resp_pack ),
--- a/rtl/cva6/common/local/util/sram_cache.sv
+++ b/rtl/cva6/common/local/util/sram_cache.sv
@@ -110,6 +110,9 @@
           .NUM_WORDS  (NUM_WORDS)
       ) data_sram (
           .clk_i  (clk_i),
+          .rst_ni (rst_ni),
+          .wuser_i(wuser_i),
+          .ruser_o(ruser_o),
           .req_i  (req_i),
           .we_i   (we_i),
           .addr_i (addr_i),
--- a/rtl/ara/hardware/src/ara_dispatcher.sv
+++ b/rtl/ara/hardware/src/ara_dispatcher.sv
@@ -1681,7 +1681,7 @@
                     end else begin
                       // Normal stripmining
                       csr_vl_d = ((|acc_req_i.rs1[$bits(acc_req_i.rs1)-1:$bits(csr_vl_d)]) ||
-                        (vlen_t'(acc_req_i.rs1) > vlmax)) ? vlmax : vlen_t'(acc_req_i.rs1);
+                        (acc_req_i.rs1[$bits(csr_vl_d)-1:0] > vlmax)) ? vlmax : acc_req_i.rs1[$bits(csr_vl_d)-1:0];
                     end
                   end
                 end
@@ -2032,7 +2032,7 @@
                     ara_req.scale_vl      = 1'b1;
                     // If stride > vl, the vslideup has no effects
                     if (|ara_req.stride[$bits(ara_req.stride)-1:$bits(csr_vl_q)] ||
-                      (vlen_t'(ara_req.stride) >= csr_vl_q)) null_vslideup = 1'b1;
+                      (ara_req.stride[$bits(csr_vl_q)-1:0] >= csr_vl_q)) null_vslideup = 1'b1;
                   end
                   6'b001111: begin
                     ara_req.op            = ara_pkg::VSLIDEDOWN;
@@ -2281,7 +2281,7 @@
                     ara_req.scale_vl      = 1'b1;
                     // If stride > vl, the vslideup has no effects
                     if (|ara_req.stride[$bits(ara_req.stride)-1:$bits(csr_vl_q)] ||
-                      (vlen_t'(ara_req.stride) >= csr_vl_q)) null_vslideup = 1'b1;
+                      (ara_req.stride[$bits(csr_vl_q)-1:0] >= csr_vl_q)) null_vslideup = 1'b1;
                   end
                   6'b001111: begin
                     ara_req.op            = ara_pkg::VSLIDEDOWN;
--- a/rtl/ara/hardware/src/vlsu/qbs/qbs_engine.sv
+++ b/rtl/ara/hardware/src/vlsu/qbs/qbs_engine.sv
@@ -952,8 +952,17 @@
   );
 
   assign compute_command_valid = state_q == QBS_ENGINE_COMPUTE_START;
-  assign compute_fault = state_q == QBS_ENGINE_COMPUTE_FAULT_DRAIN ||
-      (state_q == QBS_ENGINE_RUN && read_fault_valid);
+  // FPGA-only: one preserved LUT prevents shared-decode feedback through
+  // needed/read selection. No register, fault delay, or handshake change.
+  localparam logic [31:0] FpgaComputeFaultInit =
+      (32'h1 << int'(QBS_ENGINE_COMPUTE_FAULT_DRAIN)) |
+      (32'h1 << (16 + int'(QBS_ENGINE_COMPUTE_FAULT_DRAIN))) |
+      (32'h1 << (16 + int'(QBS_ENGINE_RUN)));
+  (* DONT_TOUCH = "TRUE" *)
+  LUT5 #(.INIT(FpgaComputeFaultInit)) i_fpga_compute_fault (
+    .I0(state_q[0]), .I1(state_q[1]), .I2(state_q[2]), .I3(state_q[3]),
+    .I4(read_fault_valid), .O(compute_fault)
+  );
 
   qbs_compute_engine i_compute_engine (
     .clk_i,
--- a/rtl/ara/hardware/src/vlsu/akv/akv_engine.sv
+++ b/rtl/ara/hardware/src/vlsu/akv/akv_engine.sv
@@ -235,6 +235,7 @@
   logic read_data_valid, read_data_ready;
   logic [AxiDataWidth-1:0] read_data;
   logic [AxiDataWidth/8-1:0] read_data_strb;
+  logic [$clog2(AxiDataWidth/8+1)-1:0] fpga_read_data_byte_count;
   logic [RangeBytesWidth-1:0] read_data_offset;
 
   // Align the beat once; each descriptor byte has a fixed write enable.
@@ -692,10 +693,21 @@
   assign replay_word_count = replay_column_q
       ? 4'((unsigned'(context_tile_count_q) + 15) >> 4)
       : 4'(unsigned'(context_head_dim_q) >> 4);
+  // Fixed-bound loops avoid dynamic $countones in Vivado 2020.1.
+  // Procedural if counts only known ones, preserving $countones X/Z behavior.
   always_comb begin
     replay_word_bytes = '0;
     for (int unsigned lane = 0; lane < NrLanes; lane++)
-      replay_word_bytes += 7'($countones(ldu_result_be_o[lane]));
+      for (int unsigned byte_lane = 0; byte_lane < 8; byte_lane++)
+        if (ldu_result_be_o[lane][byte_lane])
+          replay_word_bytes += 7'd1;
+  end
+
+  always_comb begin
+    fpga_read_data_byte_count = '0;
+    for (int unsigned byte_lane = 0; byte_lane < AxiDataWidth/8; byte_lane++)
+      if (read_data_strb[byte_lane])
+        fpga_read_data_byte_count += 1'b1;
   end
 
   always_comb begin
@@ -1055,10 +1067,10 @@
       if (read_data_fire && read_data_tag.role != AKV_RANGE_DESCRIPTOR) begin
         if (read_data_tag.role == AKV_RANGE_Q)
           q_external_bytes_o <= q_external_bytes_o +
-              32'($countones(read_data_strb));
+              32'(fpga_read_data_byte_count);
         else
           kv_external_bytes_o <= kv_external_bytes_o +
-              32'($countones(read_data_strb));
+              32'(fpga_read_data_byte_count);
       end
 
       if (state_q == AKV_ENGINE_REPLAY_WRITE) begin
--- a/rtl/board/dram_wrapper_xilinx.sv
+++ b/rtl/board/dram_wrapper_xilinx.sv
@@ -31,6 +31,7 @@
   // Controller reset
   input  logic  soc_resetn_i,
   input  logic  soc_clk_i,
+  output logic  fabric_ready_o,
   // PHY interfaces
 `ifdef USE_DDR4
   `DDR4_INTF(Ddr4CsNWidth, Ddr4DmDbiNWidth, Ddr4DqWidth, Ddr4DqsWidth)
@@ -60,7 +61,7 @@
 
 `ifdef TARGET_VCU118
   localparam dram_cfg_t cfg = '{
-    EnCdc         : 1,    // 333 MHz AXI (cf. CdcLogDepth)
+    EnCdc         : 1,    // UI clock derived by the Vivado DDR4 IP
     CdcLogDepth   : 5,
     IdWidth       : 8,
     AddrWidth     : 31,
@@ -73,7 +74,7 @@
 
 `ifdef TARGET_VCU128
   localparam dram_cfg_t cfg = '{
-    EnCdc         : 1,    // 333 MHz AXI (cf. CdcLogDepth)
+    EnCdc         : 1,    // UI clock derived by the Vivado DDR4 IP
     CdcLogDepth   : 5,
     IdWidth       : 8,
     AddrWidth     : 32,
@@ -115,6 +116,14 @@
   // Clock on which is clocked the DRAM AXI
   logic dram_axi_clk;
   logic dram_rst_o;
+  logic calib_complete;
+  logic ui_resetn;
+  // Common asynchronous assertion, separately synchronized deassertion.
+  assign fabric_ready_o = ~sys_rst_i & ~dram_rst_o & calib_complete;
+  rstgen i_ui_rstgen (
+    .clk_i(dram_axi_clk), .rst_ni(fabric_ready_o), .test_mode_i(1'b0),
+    .rst_no(ui_resetn), .init_no()
+  );
 
   // Signals before resizing
   axi_soc_req_t  soc_dresizer_req;
@@ -214,7 +223,7 @@
       .src_req_i  ( iresizer_cdc_req ),
       .src_resp_o ( iresizer_cdc_rsp ),
       .dst_clk_i  ( dram_axi_clk ),
-      .dst_rst_ni ( ~dram_rst_o  ),
+      .dst_rst_ni ( ui_resetn    ),
       .dst_req_o  ( cdc_dram_req ),
       .dst_resp_i ( cdc_dram_rsp )
     );
@@ -245,7 +254,7 @@
     // Reset
     .sys_rst                    ( sys_rst_i    ),  // Active high
     .c0_sys_clk_i               ( dram_clk_i   ),
-    .c0_ddr4_aresetn            ( soc_resetn_i ),
+    .c0_ddr4_aresetn            ( ui_resetn    ),
     // Clock and reset out
     .c0_ddr4_ui_clk             ( dram_axi_clk ),
     .c0_ddr4_ui_clk_sync_rst    ( dram_rst_o   ),
@@ -288,8 +297,8 @@
     .c0_ddr4_s_axi_rlast        ( cdc_dram_rsp.r.last   ),
     .c0_ddr4_s_axi_rvalid       ( cdc_dram_rsp.r_valid  ),
     // Others
-    .c0_init_calib_complete     ( ),
-    .addn_ui_clkout1            ( dram_clk_o ),
+    .c0_init_calib_complete     ( calib_complete ),
+    .addn_ui_clkout1            ( ),
     .dbg_clk                    ( ),
     .dbg_bus                    ( ),
     // PHY
